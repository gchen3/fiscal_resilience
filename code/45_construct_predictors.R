# 45_construct_predictors.R
# Variable construction (PREDICTORS): explanatory / control variables — legacy costs,
# social vulnerability, political environment, prior disaster experience, organizational
# capacity, budgetary institutions (see README). These come from sources other than the
# OSC finance data and may need their own import/clean steps.
#
# Writes: data/processed_data/<entity>_predictors.rds
# Register every variable in transparency/variable_registry.md.
#
# When this band grows multi-source (Census + FEMA + politics + ...), promote it to a
# 45_predictors/ subfolder with 45a_, 45b_, ... and a 45_predictors.R runner.

# TODO: implement.
